name: fitness_dashboard_ui
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  fl_chart: ^0.68.0
  flutter_svg: ^2.0.5
  flutter_staggered_grid_view: ^0.4.0 # Add this line
  intl: ^0.19.0
  google_fonts: ^6.2.1
  flutter_secure_storage: ^5.0.2
  another_flushbar: ^1.10.26

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/  
